OrgAI Demo UI (Full Canvas)
===========================

This repository contains the full App.jsx UI exactly as shown in the canvas (React single-file).
To run locally:

1. Install dependencies:
   npm install

2. Start dev server:
   npm run dev

3. Open http://localhost:5173

Note: This is a frontend demo. No backend or auth provided. Keep API keys out of frontend.
